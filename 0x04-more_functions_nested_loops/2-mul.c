#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies 2 nums
 * @a: first number in themultipliction process
 * @b: sec. number in the * process
 * Return: 0
 */

int mul(int a, int b)
{
printf("the result of multiplication = %d", a*b);
return (0);
}
