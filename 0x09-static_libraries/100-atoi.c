#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "main.h"

/**
 * _atoi - string to integer
 * @s: string
 * Return: integer of the string
 */
int atoi(const char *str);

int _atoi(char *s)
{
return (atoi(s));
}
