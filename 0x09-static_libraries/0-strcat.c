#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 *_strcat - appends a string to another one
 * @dest: the string appended to
 * @src: the source string
 * Return: The dest string after the concatination
 */

char *_strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
