#include "main.h"

/**
 * print_last_digit - prints the last digit
 * @x : a number from which we get the last digit
 * Return: the value of the last digit
 */

int print_last_digit(int x)
{
return (x % 10);
}
