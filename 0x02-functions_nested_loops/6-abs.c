#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - shows the abs of a number
 * @c: the number the be absoluted
 * Return: abs (c)
 */

int _abs(int c)
{
int x;
x = abs(c);
return x;
}
