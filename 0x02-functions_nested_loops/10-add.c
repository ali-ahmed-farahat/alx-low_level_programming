#include "main.h"
#include <stdio.h>

/**
 * add - adds to integers
 * @x: one of the two numbers to be added
 * @y: one of the two numbers to be added
 * Return: sum of x and y
 */

int add(int x, int y)
{
return (x + y);
}
